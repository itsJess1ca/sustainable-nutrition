import { NgModule } from '@angular/core';

import { ViewportHeightDirective } from './viewport-height.directive';

@NgModule({
    imports: [],
    exports: [ViewportHeightDirective],
    declarations: [ViewportHeightDirective],
    providers: [],
})
export class ViewportHeightModule { }
